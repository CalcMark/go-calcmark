# Milestone v1.0: CalcMark v1

**Status:** SHIPPED 2026-02-06
**Phases:** 1-8
**Total Plans:** 21

## Overview

CalcMark v1 delivers a polished, distributable calculation notepad for the terminal. The journey starts by fixing broken infrastructure and extracting the pure geometry layer, then rebuilds the TUI editor on a correct two-column foundation, layers on help and discoverability, adds differentiating features (autocomplete and YAML front matter), and finishes with release packaging and documentation. Every phase delivers a verifiable capability that the next phase builds upon.

## Phases

### Phase 1: Foundation

**Goal**: Project builds and tests pass on CI with current Go, dependencies are updated, and pure geometry functions are extracted and fully tested in isolation
**Depends on**: Nothing (first phase)
**Plans**: 2 plans

Plans:
- [x] 01-01: Fix CI workflow, update Go to 1.24.12, update deps, fix pre-existing test failures
- [x] 01-02: Extract pure geometry package with WrapText and CalculateRowGeometry, wire into editor

**Key Deliverables:**
- CI release workflow using go-version-file (auto-tracks go.mod)
- Go 1.24.12 with latest security patches
- Pure geometry package at cmd/calcmark/tui/geometry/
- 23 table-driven tests for geometry functions

### Phase 2: TUI Geometry & Layout

**Goal**: The two-column editor renders source and results side-by-side with correct text wrapping and vertical alignment under all scenarios
**Depends on**: Phase 1 (geometry package)
**Plans**: 2 plans

Plans:
- [x] 02-01: Write integration tests proving all five success criteria for the two-column layout
- [x] 02-02: Fix any layout failures and visually verify two-column rendering

**Key Deliverables:**
- Five integration tests validating Phase 2 success criteria (SC1-SC5)
- Confirmed all success criteria pass with no code fixes needed
- Visual verification with user feedback cataloged for future polish

### Phase 3: TUI Editor Integration

**Goal**: The editor is fully interactive with accurate cursor tracking, smooth scrolling, working evaluation pipeline, and results that update as the user types
**Depends on**: Phase 2 (correct layout rendering)
**Plans**: 5 plans

Plans:
- [x] 03-01: Cursor movement and Ctrl+Arrow word navigation with catwalk tests
- [x] 03-02: Viewport scrolling with configurable margin
- [x] 03-03: Debounced evaluation pipeline (100ms) and dependent variable updates
- [x] 03-04: Model unification: delete ModelV2, clean up references
- [x] 03-05: Interactive editor verification checkpoint

**Key Deliverables:**
- Cursor movement with word navigation (Ctrl+Arrow)
- Viewport scrolling with 3-line margin
- 100ms debounced evaluation pipeline
- Single unified Model (ModelV2 deleted)

### Phase 4: TUI Test Coverage

**Goal**: Every editor interaction has a catwalk test, and the CI pipeline contains zero flakey tests
**Depends on**: Phase 3 (editor functionally complete)
**Plans**: 3 plans

Plans:
- [x] 04-01: Fix shared document mutation and regenerate failing test expectations
- [x] 04-02: Add missing catwalk coverage (typing, narrow wrapping, long scroll) and archive VHS tapes
- [x] 04-03: Verify 10 consecutive test runs with zero failures

**Key Deliverables:**
- Dedicated test functions with fresh documents for 16 test scenarios
- VHS tapes archived to branch archive/vhs-tapes
- Zero flakey failures across 10 consecutive runs

### Phase 5: Help System

**Goal**: Users can discover all CalcMark features through CLI help commands, shell completions, an in-TUI help overlay, and an informative status bar
**Depends on**: Phase 3 (stable editor for TUI help integration)
**Plans**: 2 plans

Plans:
- [x] 05-01: CLI help commands (functions, constants) and shell completions
- [x] 05-02: TUI help overlay (F1 toggle) and status bar enhancements (line:col, EVAL...)

**Key Deliverables:**
- Function registry with metadata for all 12 CalcMark functions
- `cm help functions` and `cm help constants` commands
- Shell completions for bash/zsh/fish/powershell
- F1 help overlay and status bar with EVAL indicator

### Phase 6: Differentiators

**Goal**: Users can define document-level constants via YAML front matter and discover functions/constants/units through TUI autocomplete while typing
**Depends on**: Phase 3 (stable evaluation pipeline), Phase 5 (help infrastructure)
**Plans**: 2 plans

Plans:
- [x] 06-01: Function metadata refactor, fix function display bug, YAML error messages
- [x] 06-02: TUI autocomplete engine (functions, units, variables) and popup rendering

**Key Deliverables:**
- FunctionDef struct as single source of truth for function metadata
- YAML front matter with clear error messages
- TUI autocomplete with popup rendering
- Function parameter hints in context footer

### Phase 7: Distribution

**Goal**: Users can install CalcMark on macOS and Linux via Homebrew or download prebuilt binaries from GitHub releases for all platforms
**Depends on**: Phases 1-6 (all features complete)
**Plans**: 2 plans

Plans:
- [x] 07-01: Remove WASM infrastructure, create GoReleaser config, update CI workflow
- [x] 07-02: Validate local build, update planning docs, verify release readiness

**Key Deliverables:**
- GoReleaser configuration for 7 platform builds
- Homebrew formula generation for calcmark/homebrew-tap
- WASM infrastructure removed
- Release workflow ready for tag push

### Phase 8: Documentation

**Goal**: A new user can understand what CalcMark is, install it, and start using it within 5 minutes by reading the README and exploring example files
**Depends on**: Phases 1-7 (features and distribution complete)
**Plans**: 2 plans

Plans:
- [x] 08-01: Create example files in testdata/examples/ and visual assets (screenshot, hero GIF)
- [x] 08-02: Rewrite README with user-focused structure (what/install/quickstart/examples)

**Key Deliverables:**
- Three example .cm files (budget, unit conversion, engineering)
- TUI screenshot and hero GIF
- User-focused README with installation and quick start

---

## Milestone Summary

**Key Decisions:**
- go-version-file in CI instead of hardcoded Go version
- Pure geometry functions with zero TUI framework dependencies
- Dedicated test functions with fresh documents to avoid shared mutation
- scrollMargin = 3 lines for viewport scrolling
- evalDebounceDelay = 100ms (conservative default)
- F1 for help (not ?) to avoid conflict with calc expressions
- FunctionDef struct with init() pattern to avoid initialization cycle
- GoReleaser v2 with brews section
- Positioned CalcMark as "calculation notepad" not "Jupyter alternative"

**Issues Resolved:**
- CI release workflow Go version mismatch
- TUI two-column layout wrapping/alignment bugs
- Function result display bug in Preview pane
- VHS tape flakey tests (replaced with catwalk)
- Word navigation on macOS (Alt+b/f handling)

**Technical Debt:**
- Pre-existing modernize warnings (~39 across codebase)
- Visual polish items deferred (column headers, divider centering)
- Preview pane UX enhancement (calc outputs only) deferred to post-v1

---

*Archived: 2026-02-06 as part of v1.0 milestone completion*
