---
phase: 04-tui-test-coverage
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`task test` completes successfully 10 consecutive times with zero failures"
    - "No flaky test behavior observed across all runs"
    - "SC3 is satisfied: zero flakey failures across 10 consecutive runs"
  artifacts:
    - path: "NONE"
      provides: "Verification evidence only"
  key_links: []
---

<objective>
Verify SC3: `task test` completes with zero flakey failures across 10 consecutive runs.

Purpose: This is the final verification that the test suite is stable. After fixing shared document mutation and adding missing coverage, we need to prove the tests are consistently passing without flakiness.

Output: Evidence that all tests pass 10 consecutive times, satisfying SC3.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tui-test-coverage/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite 10 consecutive times</name>
  <files>none (verification only)</files>
  <action>
Run `task test` 10 consecutive times and record results:

```bash
for i in {1..10}; do
    echo "=== Run $i ==="
    task test 2>&1 | tail -5
    if [ $? -ne 0 ]; then
        echo "FAILED on run $i"
        exit 1
    fi
    echo "PASSED"
    echo ""
done
echo "All 10 runs passed successfully"
```

If any run fails:
1. Capture the failure output
2. Identify which test failed
3. Determine if it's a flaky failure (random) or consistent failure (bug)
4. If flaky: investigate timing, shared state, or environment issues
5. If consistent: this indicates a missed issue from 04-01 or 04-02

Record:
- Number of successful runs
- Any failures and their causes
- Total execution time for 10 runs
  </action>
  <verify>
All 10 runs complete with exit code 0.
No test failures in any run.
Output shows "PASSED" for all 10 iterations.
  </verify>
  <done>10 consecutive test runs completed with zero failures, SC3 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Document verification results</name>
  <files>none (summary output only)</files>
  <action>
Create summary documentation for the plan that includes:

1. **SC3 Evidence:**
   - Date/time of verification
   - Number of consecutive passing runs (must be 10)
   - Any observations about test stability

2. **Phase 4 Completion Checklist:**
   - SC1: Catwalk tests exist for all required interactions
     - [ ] Typing text
     - [ ] Cursor movement (arrows, home/end, page up/down)
     - [ ] Text wrapping at narrow widths (40 columns)
     - [ ] Scrolling through long documents
     - [ ] Evaluation results appearing
   - SC2: No VHS tape tests in CI (verified: VHS removed/archived)
   - SC3: Zero flakey failures across 10 consecutive runs (verified by Task 1)

3. **Test Coverage Summary:**
   - Total catwalk test files
   - Total test functions
   - All tests passing
  </action>
  <verify>
Summary includes all verification evidence.
All three Success Criteria marked as satisfied.
  </verify>
  <done>Phase 4 verification complete, all Success Criteria satisfied</done>
</task>

</tasks>

<verification>
- [ ] 10 consecutive `task test` runs completed successfully
- [ ] Zero test failures across all runs
- [ ] SC3 explicitly satisfied
- [ ] Phase 4 completion evidence documented
</verification>

<success_criteria>
- `task test` passes 10 consecutive times with zero failures
- No flaky behavior observed
- Phase 4 Success Criteria all satisfied:
  - SC1: Comprehensive catwalk coverage
  - SC2: No VHS tests in CI
  - SC3: Zero flakey failures in 10 runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-tui-test-coverage/04-03-SUMMARY.md`
</output>
