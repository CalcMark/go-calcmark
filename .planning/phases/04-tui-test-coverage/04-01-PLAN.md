---
phase: 04-tui-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/calcmark/tui/editor/catwalk_test.go
  - cmd/calcmark/tui/editor/testdata/insert_at_end
  - cmd/calcmark/tui/editor/testdata/insert_line
  - cmd/calcmark/tui/editor/testdata/scroll_navigation
  - cmd/calcmark/tui/editor/testdata/delete_empty_line
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All 4 failing tests (insert_at_end, insert_line, scroll_navigation, delete_empty_line) pass"
    - "Each failing test has its own dedicated test function with fresh document"
    - "Test expectations reflect actual correct behavior, not polluted state"
  artifacts:
    - path: "cmd/calcmark/tui/editor/catwalk_test.go"
      provides: "Dedicated test functions for insert_at_end, insert_line, scroll_navigation, delete_empty_line"
      contains: "TestEditorCatwalkInsertAtEnd"
    - path: "cmd/calcmark/tui/editor/testdata/insert_at_end"
      provides: "Regenerated test expectations"
    - path: "cmd/calcmark/tui/editor/testdata/insert_line"
      provides: "Regenerated test expectations"
    - path: "cmd/calcmark/tui/editor/testdata/scroll_navigation"
      provides: "Regenerated test expectations"
    - path: "cmd/calcmark/tui/editor/testdata/delete_empty_line"
      provides: "Regenerated test expectations"
  key_links:
    - from: "TestEditorCatwalkInsertAtEnd"
      to: "testdata/insert_at_end"
      via: "datadriven.Walk with path filter"
      pattern: "strings.HasSuffix.*insert_at_end"
---

<objective>
Fix the shared document mutation root cause in TestEditorCatwalk that causes 4 tests to fail, then regenerate test expectations with fresh state.

Purpose: Shared document between catwalk tests causes state pollution. Tests see content from previous test files (e.g., "jjjotestline 1" instead of "# Header"). The workaround (dedicated test functions) is established for 12 tests; apply it to the remaining 4 failing tests.

Output: All 20 catwalk tests pass with zero failures due to document mutation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tui-test-coverage/04-RESEARCH.md
@cmd/calcmark/tui/editor/TESTING.md
@cmd/calcmark/tui/editor/catwalk_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated test functions for failing tests</name>
  <files>cmd/calcmark/tui/editor/catwalk_test.go</files>
  <action>
Add 4 new dedicated test functions following the established pattern (e.g., TestEditorCatwalkViewportScrolling):

1. **TestEditorCatwalkInsertAtEnd** - For insert_at_end test file
   - Fresh document content appropriate for testing insert at end (lines to navigate through)
   - Filter: `strings.HasSuffix(path, "insert_at_end")`
   - Add to skipTests list in TestEditorCatwalk

2. **TestEditorCatwalkInsertLine** - For insert_line test file
   - Fresh document content for testing line insertion
   - Filter: `strings.HasSuffix(path, "insert_line")`
   - Add to skipTests list

3. **TestEditorCatwalkScrollNavigation** - For scroll_navigation test file
   - Fresh 20+ line document for testing scroll navigation (similar to TestEditorCatwalkViewportScrolling)
   - Filter: `strings.HasSuffix(path, "scroll_navigation")`
   - Add to skipTests list

4. **TestEditorCatwalkDeleteEmptyLine** - For delete_empty_line test file
   - Fresh document content appropriate for testing delete operations
   - Filter: `strings.HasSuffix(path, "delete_empty_line")`
   - Add to skipTests list

Each function should:
- Create a fresh `*document.Document` with appropriate test content
- Use `datadriven.Walk` with path filter for its specific test file
- Include standard observers: debug, lines, results (as needed)
- Follow exact pattern from TestEditorCatwalkViewportScrolling

IMPORTANT: Ensure skipTests array is updated to include all 4 test file names.
  </action>
  <verify>
Run `go test ./cmd/calcmark/tui/editor/... -run Catwalk -v 2>&1 | grep -E "(PASS|FAIL).*Catwalk"` and verify:
- TestEditorCatwalkInsertAtEnd, TestEditorCatwalkInsertLine, TestEditorCatwalkScrollNavigation, TestEditorCatwalkDeleteEmptyLine all appear
- Tests may still fail (expectations not yet regenerated) but they run
  </verify>
  <done>4 new dedicated test functions exist in catwalk_test.go, skipTests array updated, tests run without errors</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate test expectations with fresh documents</name>
  <files>
    cmd/calcmark/tui/editor/testdata/insert_at_end
    cmd/calcmark/tui/editor/testdata/insert_line
    cmd/calcmark/tui/editor/testdata/scroll_navigation
    cmd/calcmark/tui/editor/testdata/delete_empty_line
  </files>
  <action>
Regenerate expected output for all 4 test files using the -rewrite flag:

```bash
go test ./cmd/calcmark/tui/editor/... -run "TestEditorCatwalkInsertAtEnd|TestEditorCatwalkInsertLine|TestEditorCatwalkScrollNavigation|TestEditorCatwalkDeleteEmptyLine" -v -args -rewrite
```

After regeneration:
1. Run the tests WITHOUT -rewrite to confirm they pass
2. Review the regenerated expectations to verify they make sense:
   - insert_at_end: Should show fresh document content, not "jjjotestline 1"
   - insert_line: Should show proper line insertion behavior
   - scroll_navigation: Should show scroll state with 20+ line document
   - delete_empty_line: Should show proper delete behavior

If any test file expectations look wrong (still showing polluted content), check that the dedicated test function creates a fresh document correctly.
  </action>
  <verify>
Run `go test ./cmd/calcmark/tui/editor/... -run Catwalk -v 2>&1 | grep -E "(PASS|FAIL)"` and verify all tests pass.
Run `head -30 cmd/calcmark/tui/editor/testdata/insert_at_end` and verify the debug output shows clean content (no "jjj" prefix pollution).
  </verify>
  <done>All 4 previously failing tests now pass, regenerated expectations reflect correct (unpolluted) behavior</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify zero failures</name>
  <files>none (verification only)</files>
  <action>
Run the complete catwalk test suite and verify all tests pass:

```bash
go test ./cmd/calcmark/tui/editor/... -run Catwalk -v
```

Count the results and verify:
- All 20 catwalk tests pass (16 existing + 4 fixed)
- No test failures
- No test pollution issues

Also run the full test suite to ensure no regressions:

```bash
task test 2>&1 | tail -20
```
  </action>
  <verify>
`go test ./cmd/calcmark/tui/editor/... -run Catwalk -v 2>&1 | grep -c PASS` returns 20+
`go test ./cmd/calcmark/tui/editor/... -run Catwalk -v 2>&1 | grep -c FAIL` returns 0
  </verify>
  <done>All catwalk tests pass, full test suite has no regressions</done>
</task>

</tasks>

<verification>
- [ ] `go test ./cmd/calcmark/tui/editor/... -run Catwalk -v` shows all tests passing
- [ ] No test output shows polluted content like "jjjotestline"
- [ ] 4 new dedicated test functions exist in catwalk_test.go
- [ ] skipTests array includes all 4 test file names
- [ ] `task test` passes (no regressions)
</verification>

<success_criteria>
- All 4 previously failing tests (insert_at_end, insert_line, scroll_navigation, delete_empty_line) now pass
- Root cause (shared document mutation) is fixed via dedicated test functions pattern
- Test expectations reflect actual correct behavior
- Full test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-tui-test-coverage/04-01-SUMMARY.md`
</output>
