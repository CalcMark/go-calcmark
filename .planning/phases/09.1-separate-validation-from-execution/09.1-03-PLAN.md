---
phase: 09.1-separate-validation-from-execution
plan: 03
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - cmd/calcmark/tui/editor/error_line_display_test.go
  - cmd/calcmark/tui/editor/function_result_display_test.go
  - cmd/calcmark/tui/editor/catwalk_wrapping_test.go
  - cmd/calcmark/tui/editor/catwalk_type_mismatch_test.go
  - cmd/calcmark/tui/editor/edit_redefinition_bug_test.go
  - cmd/calcmark/tui/editor/redefinition_line_number_test.go
  - cmd/calcmark/tui/editor/redefinition_error_test.go
  - cmd/calcmark/tui/editor/redefinition_display_test.go
  - cmd/calcmark/tui/editor/error_wrong_line_test.go
autonomous: true

must_haves:
  truths:
    - "All TUI editor tests pass using impl/document.Evaluator"
    - "Tests verify the same behaviors as before migration"
    - "No test files call doc.Evaluate() directly"
  artifacts:
    - path: "cmd/calcmark/tui/editor/error_line_display_test.go"
      provides: "Test for error line positioning"
      contains: "impldoc.NewEvaluator"
    - path: "cmd/calcmark/tui/editor/redefinition_error_test.go"
      provides: "Test for redefinition error display"
      contains: "impldoc.NewEvaluator"
  key_links:
    - from: "cmd/calcmark/tui/editor/*_test.go"
      to: "impl/document"
      via: "test imports"
      pattern: 'import.*"github.com/CalcMark/go-calcmark/impl/document"'
---

<objective>
Migrate TUI editor test files to use impl/document.Evaluator

Purpose: After Plan 01 removes doc.Evaluate() from spec/document, TUI editor test files that
called this method need to be updated. These tests verify error display, line positioning, and
other TUI behaviors that depend on document evaluation.

Output: All TUI editor tests compile and pass using impl/document.Evaluator for evaluation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-separate-validation-from-execution/09.1-01-SUMMARY.md
@impl/document/evaluator.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TUI editor test files to use impl/document.Evaluator</name>
  <files>
    cmd/calcmark/tui/editor/error_line_display_test.go
    cmd/calcmark/tui/editor/function_result_display_test.go
    cmd/calcmark/tui/editor/catwalk_wrapping_test.go
    cmd/calcmark/tui/editor/catwalk_type_mismatch_test.go
    cmd/calcmark/tui/editor/edit_redefinition_bug_test.go
    cmd/calcmark/tui/editor/redefinition_line_number_test.go
    cmd/calcmark/tui/editor/redefinition_error_test.go
    cmd/calcmark/tui/editor/redefinition_display_test.go
    cmd/calcmark/tui/editor/error_wrong_line_test.go
  </files>
  <action>
    For each TUI test file that calls doc.Evaluate():

    1. Add import:
       ```go
       impldoc "github.com/CalcMark/go-calcmark/impl/document"
       ```

    2. Replace pattern:
       ```go
       // Before:
       doc, err := document.NewDocument(source)
       doc.Evaluate()  // often ignores error for negative tests

       // After:
       doc, err := document.NewDocument(source)
       eval := impldoc.NewEvaluator()
       eval.Evaluate(doc)  // still ignoring error if that's the test intent
       ```

    3. Some tests may expect evaluation to fail - preserve that behavior:
       ```go
       // Before:
       err = doc.Evaluate()
       if err == nil { t.Error("expected error") }

       // After:
       eval := impldoc.NewEvaluator()
       err = eval.Evaluate(doc)
       if err == nil { t.Error("expected error") }
       ```

    Specific files to update:

    a) error_line_display_test.go:
       - TestErrorLineDisplaysAtCorrectPosition calls doc.Evaluate()
       - Test intentionally triggers error to verify display position

    b) function_result_display_test.go:
       - Tests function results display, likely calls Evaluate

    c) catwalk_wrapping_test.go, catwalk_type_mismatch_test.go:
       - Catwalk tests, update Evaluate calls

    d) edit_redefinition_bug_test.go:
       - Tests redefinition during editing

    e) redefinition_line_number_test.go, redefinition_error_test.go, redefinition_display_test.go:
       - Tests for redefinition error display

    f) error_wrong_line_test.go:
       - Tests error appearing at wrong line (bug reproduction)
  </action>
  <verify>
    Run: go test ./cmd/calcmark/tui/editor/... -v -run "Error|Redefinition|Catwalk"

    Expected: All matched tests pass.

    Run: grep -l "doc\.Evaluate()" cmd/calcmark/tui/editor/*_test.go

    Expected: No files returned (all Evaluate calls migrated)
  </verify>
  <done>
    - All 9 TUI editor test files migrated
    - No test file calls doc.Evaluate() directly
    - Tests compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify no regressions</name>
  <files>
    cmd/calcmark/tui/editor/
  </files>
  <action>
    1. Run full TUI editor test suite:
       go test ./cmd/calcmark/tui/editor/... -v -count=1

    2. Run the full project test suite:
       go test ./... -count=1

    3. Verify key behaviors still work:
       - Error displays at correct line position
       - Redefinition errors show proper diagnostics
       - Function results display correctly

    4. Check for any remaining doc.Evaluate() calls in the entire codebase:
       grep -r "doc\.Evaluate()" --include="*.go" .

       Expected: Only in impl/document/evaluator_test.go (if any), which is correct.
  </action>
  <verify>
    Run: go test ./... -count=1

    Expected: All tests pass.

    Run: grep -r "\.Evaluate()" --include="*.go" . | grep -v "impl/document" | grep -v "_test.go" | grep "spec/document"

    Expected: No output (no Evaluate calls in spec/document source)
  </verify>
  <done>
    - All project tests pass: go test ./... returns PASS
    - No doc.Evaluate() calls remain in spec/document (except maybe evaluator patterns)
    - Architecture violation completely resolved
  </done>
</task>

</tasks>

<verification>
1. `go test ./cmd/calcmark/tui/editor/...` passes all tests
2. `go test ./...` passes all tests
3. `grep "doc\.Evaluate()" cmd/calcmark/tui/editor/*_test.go` returns no matches
4. No architecture violations: spec/ has no imports from impl/
</verification>

<success_criteria>
- All 9 TUI editor test files migrated
- All tests pass after migration
- Full project test suite passes
- Architecture rule enforced across entire codebase
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-separate-validation-from-execution/09.1-03-SUMMARY.md`
</output>
