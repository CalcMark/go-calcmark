---
phase: 09.1-separate-validation-from-execution
plan: 02
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - spec/document/eval_result_test.go
  - spec/document/semantic_test.go
  - spec/document/diagnostic_line_debug_test.go
  - spec/document/redefinition_across_blocks_test.go
  - spec/document/redefinition_diagnostic_test.go
  - spec/document/eval_flow_debug_test.go
autonomous: true

must_haves:
  truths:
    - "All spec/document tests pass using impl/document.Evaluator"
    - "Tests verify the same behaviors as before migration"
    - "No test files import spec/document just to call Evaluate()"
  artifacts:
    - path: "spec/document/eval_result_test.go"
      provides: "Tests for document evaluation storing results"
      contains: "impl/document"
    - path: "spec/document/semantic_test.go"
      provides: "Tests for semantic validation (redefinition)"
      contains: "impl/document"
  key_links:
    - from: "spec/document/*_test.go"
      to: "impl/document"
      via: "test imports"
      pattern: 'import.*"github.com/CalcMark/go-calcmark/impl/document"'
---

<objective>
Migrate spec/document test files to use impl/document.Evaluator

Purpose: After Plan 01 removes doc.Evaluate() from spec/document, all test files that called
this method need to be updated. These tests verify document evaluation behavior and should
use the canonical impl/document.Evaluator API.

Output: All spec/document tests compile and pass using impl/document.Evaluator for evaluation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-separate-validation-from-execution/09.1-01-SUMMARY.md
@impl/document/evaluator.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate spec/document test files to use impl/document.Evaluator</name>
  <files>
    spec/document/eval_result_test.go
    spec/document/semantic_test.go
    spec/document/diagnostic_line_debug_test.go
    spec/document/redefinition_across_blocks_test.go
    spec/document/redefinition_diagnostic_test.go
    spec/document/eval_flow_debug_test.go
  </files>
  <action>
    For each test file that calls doc.Evaluate():

    1. Add import:
       ```go
       impldoc "github.com/CalcMark/go-calcmark/impl/document"
       ```

    2. Replace pattern:
       ```go
       // Before:
       doc, err := document.NewDocument(source)
       err = doc.Evaluate()

       // After:
       doc, err := document.NewDocument(source)
       eval := impldoc.NewEvaluator()
       err = eval.Evaluate(doc)
       ```

    3. For tests that check EvaluateBlock:
       ```go
       // Before:
       err = doc.EvaluateBlock(blockID)

       // After:
       eval := impldoc.NewEvaluator()
       err = eval.EvaluateBlock(doc, blockID)
       ```

    Specific files to update:

    a) spec/document/eval_result_test.go:
       - TestDocumentEvaluationStoresResults calls doc.Evaluate()
       - Update to use impl/document.NewEvaluator().Evaluate(doc)

    b) spec/document/semantic_test.go:
       - TestVariableRedefinitionRejectedInDocument calls doc.Evaluate()
       - TestVariableDefinitionAllowedInDocument calls doc.Evaluate()
       - Update both to use impl/document.NewEvaluator().Evaluate(doc)

    c) spec/document/diagnostic_line_debug_test.go:
       - Debug test, update if it calls doc.Evaluate()

    d) spec/document/redefinition_across_blocks_test.go:
       - Redefinition tests, update calls to doc.Evaluate()

    e) spec/document/redefinition_diagnostic_test.go:
       - Redefinition diagnostic tests, update calls

    f) spec/document/eval_flow_debug_test.go:
       - Debug test for eval flow, update if needed
  </action>
  <verify>
    Run: go test ./spec/document/... -v

    Expected: All tests pass.

    Run: grep -l "doc\.Evaluate()" spec/document/*_test.go

    Expected: No files returned (all Evaluate calls migrated)
  </verify>
  <done>
    - All spec/document test files use impl/document.Evaluator
    - All tests pass: go test ./spec/document/... returns PASS
    - No test file calls doc.Evaluate() directly
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify test coverage maintained</name>
  <files>
    spec/document/
  </files>
  <action>
    1. Run full test suite for spec/document:
       go test ./spec/document/... -v -count=1

    2. Verify each migrated test still tests the intended behavior:
       - eval_result_test.go: Document evaluation stores LastValue
       - semantic_test.go: Variable redefinition rejected/allowed
       - redefinition tests: Cross-block redefinition handling

    3. Check that doc.go doesn't reference Evaluate in its documentation:
       - Update doc.go if it mentions doc.Evaluate() in examples
       - Examples should show using impl/document.Evaluator

    4. Run project-wide tests to ensure no regressions:
       go test ./... -count=1
  </action>
  <verify>
    Run: go test ./spec/document/... -count=1

    Expected: All tests pass.

    Run: go test ./... -count=1 2>&1 | tail -20

    Expected: Tests pass (TUI tests may fail if not yet migrated, that's Plan 03)
  </verify>
  <done>
    - All spec/document tests pass
    - Test behaviors unchanged (same assertions, different API)
    - Documentation updated if needed
  </done>
</task>

</tasks>

<verification>
1. `go test ./spec/document/...` passes all tests
2. `grep "doc\.Evaluate()" spec/document/*_test.go` returns no matches
3. All test files use impl/document.Evaluator pattern
</verification>

<success_criteria>
- All 6 spec/document test files migrated
- All tests pass after migration
- Tests verify same behaviors (redefinition, results storage, etc.)
- No direct calls to doc.Evaluate() in spec/document tests
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-separate-validation-from-execution/09.1-02-SUMMARY.md`
</output>
