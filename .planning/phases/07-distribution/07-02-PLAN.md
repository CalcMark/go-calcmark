---
phase: 07-distribution
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false

user_setup:
  - service: github
    why: "Homebrew tap repository and PAT for cross-repo push"
    env_vars:
      - name: HOMEBREW_TAP_GITHUB_TOKEN
        source: "GitHub Settings -> Developer settings -> Personal access tokens -> Generate new token (classic) with 'repo' scope"
    dashboard_config:
      - task: "Create CalcMark/homebrew-tap repository"
        location: "GitHub -> New repository -> Owner: CalcMark, Name: homebrew-tap, Public"

must_haves:
  truths:
    - "Local goreleaser build produces working binaries for current platform"
    - "Binary version matches git tag when built with ldflags"
    - "Roadmap success criteria no longer mentions WASM"
    - "Requirements list no longer includes WASM or Scoop items"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 7 success criteria without WASM"
      contains: "brew install"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements without WASM/Scoop"
      contains: "DIST-01"
  key_links:
    - from: ".goreleaser.yaml"
      to: "cmd/calcmark/main.go"
      via: "Version injection via ldflags"
      pattern: "main.Version"
---

<objective>
Validate the release pipeline locally and update planning documents to reflect WASM removal.

Purpose: Ensure the GoReleaser configuration works end-to-end before attempting a real release, and align planning documents with the decisions made in CONTEXT.md (WASM removal, no Scoop, no shell completions in archives).

Output: Verified local build, updated ROADMAP.md and REQUIREMENTS.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-distribution/07-CONTEXT.md
@.planning/phases/07-distribution/07-01-SUMMARY.md
@.goreleaser.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate local GoReleaser build</name>
  <files>None (validation only)</files>
  <action>
Run local validation to ensure GoReleaser configuration works correctly:

1. Run `goreleaser check` to validate configuration syntax

2. Run `goreleaser build --snapshot --clean` to build all binaries:
   - This creates binaries in `dist/` without publishing
   - Snapshot mode doesn't require a git tag

3. Test the built binary for current platform:
   - On macOS arm64: `./dist/cm_darwin_arm64/cm version`
   - On macOS amd64: `./dist/cm_darwin_amd64_v1/cm version`
   - Verify version shows "0.0.0-SNAPSHOT-..." (snapshot mode)

4. Verify all 7 platform binaries exist:
   - cm_darwin_amd64_v1/cm
   - cm_darwin_arm64/cm
   - cm_linux_amd64_v1/cm
   - cm_linux_arm64/cm
   - cm_linux_arm_6/cm
   - cm_windows_amd64_v1/cm.exe
   - cm_windows_arm64/cm.exe

5. Clean up dist directory after validation:
   - `rm -rf dist/`
  </action>
  <verify>
- `goreleaser check` exits with code 0
- `goreleaser build --snapshot --clean` exits with code 0
- Binary count: `ls -d dist/cm_* | wc -l` returns 7
- Current platform binary runs: `./dist/cm_darwin_arm64/cm version` shows version
  </verify>
  <done>GoReleaser builds all 7 platform binaries successfully and current platform binary is executable</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md success criteria</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update Phase 7 section in ROADMAP.md to reflect CONTEXT.md decisions:

1. Update **Goal** (line ~124) to remove WASM reference:
   - OLD: "Users can install CalcMark on macOS, Linux, and Windows via a single command or prebuilt binary download, with all release artifacts signed and verified"
   - NEW: "Users can install CalcMark on macOS and Linux via Homebrew or download prebuilt binaries from GitHub releases for all platforms"

2. Update **Requirements** list to remove WASM and Scoop:
   - Remove: DIST-06 (Scoop bucket)
   - Remove: DIST-07 (Man pages)
   - Remove: DIST-08 (Shell completions in archives)
   - Remove: DIST-09 (WASM optimized)
   - Remove: DIST-10 (WASM in releases)
   - Keep: DIST-01, DIST-02, DIST-03, DIST-04, DIST-05, DIST-11, DIST-12

3. Update **Success Criteria** to match CONTEXT.md:
   - SC1: Keep Homebrew install (change <tap> to CalcMark/homebrew-tap)
   - SC2: Keep Linux tarball download
   - SC3: Keep Windows zip download
   - SC4: Keep checksums
   - SC5: REMOVE WASM criterion entirely

4. Update **Plans** section:
   - Change from 3 plans to 2 plans
   - Update plan descriptions to match actual plans:
     - 07-01: WASM removal, GoReleaser config, CI workflow
     - 07-02: Local validation and documentation updates
   - Remove 07-03 (WASM optimization - no longer needed)
  </action>
  <verify>
- `grep -c "WASM" .planning/ROADMAP.md` returns 0 in Phase 7 section
- `grep -c "Scoop" .planning/ROADMAP.md` returns 0 in Phase 7 section
- `grep "Plans:" .planning/ROADMAP.md` shows "2 plans" for Phase 7
- Phase 7 success criteria has 4 items (not 5)
  </verify>
  <done>ROADMAP.md Phase 7 section updated to reflect WASM removal and 2-plan structure</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update Distribution requirements to match CONTEXT.md decisions:

1. In Distribution section, mark as out-of-scope or remove:
   - DIST-06: "Scoop bucket configured and working" -> Remove or mark as N/A
   - DIST-07: "Man pages generated and bundled" -> Remove or mark as N/A
   - DIST-08: "Shell completion files bundled in releases" -> Remove or mark as N/A
   - DIST-09: "WASM build optimized and size monitored" -> Remove or mark as N/A
   - DIST-10: "WASM binary included in GitHub releases" -> Remove or mark as N/A

2. Add note under Distribution section explaining scope change:
   "Note: Per Phase 7 CONTEXT.md, Scoop, WASM, man pages, and bundled shell completions are out of scope. Users can generate shell completions via `cm completion bash/zsh/fish`."

3. Update traceability table to reflect removed requirements

4. Update coverage count at bottom of file
  </action>
  <verify>
- Distribution section shows 7 active requirements (DIST-01 through DIST-05, DIST-11, DIST-12)
- Removed requirements are clearly marked or deleted
- Traceability table matches
  </verify>
  <done>REQUIREMENTS.md updated to reflect actual Phase 7 scope without WASM, Scoop, or bundled completions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify release readiness</name>
  <what-built>
Complete GoReleaser pipeline ready for first tagged release:
- GoReleaser configuration (.goreleaser.yaml)
- GitHub Actions workflow (.github/workflows/release.yml)
- Local builds validated for all 7 platforms
- WASM infrastructure removed
- Planning docs updated
  </what-built>
  <how-to-verify>
1. **GoReleaser validation:**
   - Run `goreleaser check` - should pass with no errors

2. **Local build test:**
   - Run `goreleaser build --snapshot --clean`
   - Verify 7 binary directories exist in dist/

3. **Binary functionality:**
   - Run `./dist/cm_darwin_arm64/cm version` (or your platform)
   - Should show version info

4. **Before real release (user setup required):**
   - Create `CalcMark/homebrew-tap` repository on GitHub (public, empty)
   - Create Personal Access Token with `repo` scope
   - Add as repository secret `HOMEBREW_TAP_GITHUB_TOKEN` to go-calcmark repo

5. **First release test (optional):**
   - Tag with `git tag v0.3.0` (or appropriate version)
   - Push tag: `git push origin v0.3.0`
   - Watch GitHub Actions for release job
   - Verify GitHub Releases page shows all artifacts

Note: The Homebrew tap push will fail if the tap repository doesn't exist
or the PAT secret isn't configured. This is expected until user setup.
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Local build works:**
   - `goreleaser check` passes
   - `goreleaser build --snapshot --clean` produces 7 binaries

2. **Documentation updated:**
   - ROADMAP.md Phase 7 has no WASM references
   - REQUIREMENTS.md has 7 active DIST requirements

3. **Ready for release:**
   - Configuration complete
   - Workflow updated
   - User understands setup steps for Homebrew tap
</verification>

<success_criteria>
- Local GoReleaser build produces working binaries for all 7 platforms
- ROADMAP.md reflects actual phase scope (no WASM, no Scoop)
- REQUIREMENTS.md updated with correct DIST requirements
- User has verified release readiness and understands Homebrew tap setup
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution/07-02-SUMMARY.md`
</output>
