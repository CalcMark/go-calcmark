---
phase: 03-tui-editor-integration
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - cmd/calcmark/tui/editor/model_v2.go
  - cmd/calcmark/tui/editor/model_v2_layout_test.go
  - cmd/calcmark/tui/editor/sidebyside.go
  - cmd/calcmark/tui/editor/sidebyside_test.go
autonomous: true

must_haves:
  truths:
    - "Single unified Model implementation exists"
    - "Incomplete ModelV2 implementation is deleted"
    - "All references to ModelV2 are removed from code"
    - "All tests pass after cleanup"
  artifacts:
    - path: "cmd/calcmark/tui/editor/model.go"
      provides: "Single Model implementation with full editor functionality"
    - path: "cmd/calcmark/tui/editor/model_v2.go"
      provides: "FILE DELETED - incomplete implementation removed"
  key_links:
    - from: "editor package"
      to: "Model type"
      via: "All code paths use Model"
      pattern: "Model\\{"
---

<objective>
Delete ModelV2 and all related code, leaving Model as the single, unified editor implementation.

Purpose: Unify the editor to a single Model implementation per user intent ("single implementation with clear naming, delete the one we don't need").

**Research-informed decision:** CONTEXT.md stated "Rename ModelV2 to Model" based on the assumption that ModelV2 was the more complete implementation. However, research revealed the opposite:
- Model (model.go): 2003 lines, COMPLETE - has cursor tracking, edit buffer, debounce, dirty state, save prompt, quit confirmation
- ModelV2 (model_v2.go): 665 lines, INCOMPLETE - uses textarea but lacks many features

The user's INTENT was "single unified implementation, delete the incomplete one." The research showed Model is complete and ModelV2 is incomplete. Therefore, the correct action is: keep Model (it's complete), delete ModelV2 (it's incomplete). No renaming needed.

Output: Clean codebase with only Model remaining, all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tui-editor-integration/03-CONTEXT.md
@.planning/phases/03-tui-editor-integration/03-RESEARCH.md
@cmd/calcmark/tui/editor/model.go
@cmd/calcmark/tui/editor/model_v2.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify all ModelV2 references</name>
  <files>cmd/calcmark/tui/editor/</files>
  <action>
Search the codebase for all references to ModelV2:

```bash
grep -r "ModelV2" cmd/calcmark/tui/editor/
grep -r "model_v2" cmd/calcmark/tui/editor/
```

Document all files that reference ModelV2:
- model_v2.go (the main file to delete)
- model_v2_layout_test.go (test file for ModelV2)
- sidebyside.go (may have ModelV2 rendering)
- sidebyside_test.go (may have ModelV2 tests)
- Any other files

Create a list of files to modify or delete.
Do NOT make changes yet - this task is analysis only.
  </action>
  <verify>
List of files with ModelV2 references is documented.
  </verify>
  <done>
Complete list of ModelV2 references identified across all files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete ModelV2 and related files</name>
  <files>
cmd/calcmark/tui/editor/model_v2.go
cmd/calcmark/tui/editor/model_v2_layout_test.go
  </files>
  <action>
Delete the ModelV2 implementation files:

1. Delete `model_v2.go` - the incomplete ModelV2 implementation (665 lines)
2. Delete `model_v2_layout_test.go` - tests specific to ModelV2

Use `git rm` to properly track the deletion:
```bash
git rm cmd/calcmark/tui/editor/model_v2.go
git rm cmd/calcmark/tui/editor/model_v2_layout_test.go
```

These files are safe to delete because:
- Model in model.go has all needed functionality (2003 lines, per research findings)
- ModelV2 was experimental, uses textarea, and is incomplete
- User intent: "single implementation, delete the one we don't need"
- Research confirmed: Model is complete, ModelV2 is incomplete
  </action>
  <verify>
Run `ls cmd/calcmark/tui/editor/model_v2*` - should show "No such file or directory"
Run `go build ./cmd/calcmark/...` - should compile (no broken imports)
  </verify>
  <done>
model_v2.go and model_v2_layout_test.go are deleted.
Build still succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up remaining ModelV2 references</name>
  <files>
cmd/calcmark/tui/editor/sidebyside.go
cmd/calcmark/tui/editor/sidebyside_test.go
  </files>
  <action>
Check if sidebyside.go and sidebyside_test.go have ModelV2 references:

1. If sidebyside.go has ModelV2-specific rendering functions, remove them
2. If sidebyside_test.go has ModelV2 tests, remove them
3. Remove any imports that were only used by ModelV2 code (e.g., bubbles/textarea if no longer needed)

Also check:
- Any .md documentation files that reference ModelV2 as current (update to note it's removed)
- TEXTAREA_INTEGRATION.md, TWO_PANE_DESIGN.md - these can stay as historical docs

Run tests after cleanup:
```bash
go test ./cmd/calcmark/tui/editor/... -v
```

If tests fail due to missing ModelV2:
- Delete the failing tests (they tested removed code)
- Do NOT recreate ModelV2 to make tests pass
  </action>
  <verify>
Run `grep -r "ModelV2" cmd/calcmark/tui/editor/` - should return no results
Run `go build ./cmd/calcmark/...` - should succeed
Run `go test ./cmd/calcmark/tui/editor/... -v` - should pass
  </verify>
  <done>
No ModelV2 references remain in editor package code.
Build and all tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "ModelV2" cmd/calcmark/tui/editor/` returns no results (except .md docs if kept for history)
2. `go build ./cmd/calcmark/...` succeeds
3. `go test ./cmd/calcmark/tui/editor/... -v` passes all tests
4. `task test` passes (full test suite)
5. `task quality` passes (no linting issues from cleanup)
</verification>

<success_criteria>
- model_v2.go is deleted from the repository
- All ModelV2 tests are deleted or updated
- No code references to ModelV2 remain
- Model is the only editor implementation
- All tests pass after cleanup
- Build succeeds with no broken imports
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-editor-integration/03-04-SUMMARY.md`
</output>
