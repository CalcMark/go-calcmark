---
phase: 03-tui-editor-integration
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - cmd/calcmark/tui/editor/model.go
  - cmd/calcmark/tui/editor/testdata/evaluation_debounce
  - cmd/calcmark/tui/editor/testdata/dependent_results
autonomous: true

must_haves:
  truths:
    - "Typing triggers re-evaluation after ~100ms debounce pause"
    - "Results update in preview pane when calculation changes"
    - "Changing a variable updates all dependent results"
    - "Previous results remain visible during evaluation (no flicker)"
    - "Non-calculation lines show blank in results pane"
  artifacts:
    - path: "cmd/calcmark/tui/editor/model.go"
      provides: "Configurable debounce delay and evaluation pipeline"
      contains: "evalDebounceDelay.*100"
    - path: "cmd/calcmark/tui/editor/testdata/evaluation_debounce"
      provides: "Catwalk tests for debounced evaluation"
    - path: "cmd/calcmark/tui/editor/testdata/dependent_results"
      provides: "Catwalk tests for dependent variable updates"
  key_links:
    - from: "model.go debounceUpdate"
      to: "evalDebounceMsg handler"
      via: "tea.Tick with snapshot"
      pattern: "tea.Tick.*evalDebounce"
    - from: "model.go transitionToProcessing"
      to: "reEvaluate"
      via: "state transition triggers eval"
      pattern: "reEvaluate"
---

<objective>
Configure evaluation debounce to ~100ms and verify the evaluation pipeline correctly updates results, including dependent variables.

Purpose: Live evaluation is a core CalcMark feature. Users expect to see results update as they type, but not so fast that it causes flicker or performance issues.
Output: Working debounced evaluation with catwalk tests proving correct behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tui-editor-integration/03-CONTEXT.md
@.planning/phases/03-tui-editor-integration/03-RESEARCH.md
@cmd/calcmark/tui/editor/model.go
@cmd/calcmark/tui/editor/results.go
@cmd/calcmark/tui/editor/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update debounce delay to 100ms</name>
  <files>cmd/calcmark/tui/editor/model.go</files>
  <action>
Find the evalDebounceDelay constant at the top of model.go:

```go
const evalDebounceDelay = 50 * time.Millisecond
```

Change to:

```go
// evalDebounceDelay is the time to wait after typing stops before re-evaluating.
// This prevents excessive evaluation during rapid typing while keeping results responsive.
// Can be tuned lower if interpreter performance allows (100ms is conservative).
const evalDebounceDelay = 100 * time.Millisecond
```

This matches the user decision in CONTEXT.md: "Results update on a ~100ms debounce after typing pauses".

The value is already documented as configurable in code per the decision.
  </action>
  <verify>
Run `go build ./cmd/calcmark/...` to confirm compilation.
Grep for evalDebounceDelay to confirm the change: `grep evalDebounceDelay cmd/calcmark/tui/editor/model.go`
  </verify>
  <done>
evalDebounceDelay is set to 100ms with documentation comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create catwalk tests for evaluation debounce</name>
  <files>cmd/calcmark/tui/editor/testdata/evaluation_debounce</files>
  <action>
Create a new catwalk test file `testdata/evaluation_debounce` that verifies:

1. **Typing a calculation triggers evaluation**
   - Start with empty or simple document
   - Type "= 2 + 2"
   - Use `observe=results` to verify result shows "4"

2. **Results appear in preview pane**
   - Type "x = 10"
   - Verify x is evaluated and shows 10 in results
   - Type "= x * 2"
   - Verify result shows 20

3. **Non-calculation lines show blank in results**
   - Type "This is just text" (prose, not calculation)
   - Verify results pane is blank for that line

4. **Editing preserves partial results**
   - Type a calculation
   - Verify result appears
   - Add more characters (simulating typing)
   - Verify previous result is still visible (no blank/flicker)

Note: Catwalk tests run synchronously, so debounce timing is handled by the test harness.
Focus on verifying the evaluation logic, not the exact timing.

Use `observe=results` directive to see LineResult values.
Use `observe=view` to see rendered output if needed.
  </action>
  <verify>
Run `go test ./cmd/calcmark/tui/editor/... -run TestEditorCatwalk -v` and verify evaluation_debounce tests pass.
  </verify>
  <done>
catwalk test file exists at testdata/evaluation_debounce.
Basic evaluation scenarios pass including prose vs calculations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create catwalk tests for dependent variable updates</name>
  <files>cmd/calcmark/tui/editor/testdata/dependent_results</files>
  <action>
Create a new catwalk test file `testdata/dependent_results` that verifies:

1. **Changing a variable updates dependent lines**
   - Start with document:
     ```
     tax = 10%
     price = 100
     total = price * (1 + tax)
     ```
   - Verify total shows 110
   - Navigate to tax line
   - Change tax to 20%
   - Verify total now shows 120

2. **Multiple dependents update correctly**
   - Create document with one variable used by multiple lines
   - Change the source variable
   - Verify all dependent lines update

3. **Circular/error handling**
   - Create an error condition (e.g., undefined variable)
   - Verify error is shown in results pane
   - Fix the error
   - Verify result appears

Use `observe=results` to check LineResult values.
Use the key simulation to navigate and edit (arrow keys, backspace, type).

Reference CONTEXT.md decision: "Errors show both: indicator (X) in source pane AND full diagnostic message in results/preview pane"
  </action>
  <verify>
Run `go test ./cmd/calcmark/tui/editor/... -run TestEditorCatwalk -v` and verify dependent_results tests pass.
  </verify>
  <done>
catwalk test file exists at testdata/dependent_results.
Variable dependency updates verified via catwalk tests.
Error display behavior verified.
  </done>
</task>

</tasks>

<verification>
1. `go build ./cmd/calcmark/...` succeeds
2. `go test ./cmd/calcmark/tui/editor/... -v` passes all tests
3. `task test` passes (full test suite)
4. evalDebounceDelay is 100ms (grep confirms)
5. New catwalk tests in testdata/evaluation_debounce and testdata/dependent_results exist and pass
</verification>

<success_criteria>
- Debounce delay is 100ms (configurable constant)
- Typing triggers re-evaluation after debounce
- Results update correctly in preview pane
- Dependent variables update when source changes
- Non-calculation lines show blank in results
- No flicker during evaluation (previous results remain visible)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-editor-integration/03-03-SUMMARY.md`
</output>
