---
phase: 11-navigation
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - cmd/calcmark/tui/editor/testdata/word_nav_comprehensive
autonomous: true

must_haves:
  truths:
    - "Alt+B moves cursor one word left (equivalent to Ctrl+Left)"
    - "Alt+F moves cursor one word right (equivalent to Ctrl+Right)"
    - "Word navigation wraps to previous/next line at boundaries"
  artifacts:
    - path: "cmd/calcmark/tui/editor/testdata/word_nav_comprehensive"
      provides: "Comprehensive catwalk test for Alt+B/F word navigation"
      contains: "alt+b"
  key_links:
    - from: "model.go handleDefaultKey() Alt handling"
      to: "handleCtrlLeftKey()/handleCtrlRightKey()"
      via: "Alt+b/f rune matching"
      pattern: "case 'b', 'B'"
---

<objective>
Create comprehensive tests validating all NAV-01/NAV-02 requirements including Alt+B/F bindings.

Purpose: NAV-01 and NAV-02 require both Ctrl+Arrow AND Alt+B/F for word navigation. The implementation exists but tests only cover Ctrl+Arrow. Alt+B/F is critical for macOS where Ctrl+Arrow is captured by the OS.

Output: Comprehensive catwalk test proving Alt+B/F work identically to Ctrl+Arrow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-navigation/11-RESEARCH.md

# Key files
@cmd/calcmark/tui/editor/model.go (lines 443-464: Alt key handling)
@cmd/calcmark/tui/editor/testdata/word_movement (existing Ctrl+Arrow tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive Alt+B/F word navigation test</name>
  <files>cmd/calcmark/tui/editor/testdata/word_nav_comprehensive</files>
  <action>
    Create a new catwalk test file that verifies Alt+B and Alt+F word navigation.
    This supplements the existing word_movement test which only covers Ctrl+Arrow.

    The test should verify:
    1. Alt+B moves backward by one word (same as Ctrl+Left)
    2. Alt+F moves forward by one word (same as Ctrl+Right)
    3. Alt+B at line start wraps to end of previous line
    4. Alt+F at line end wraps to start of next line
    5. Word boundaries respect punctuation and whitespace

    Test structure:
    ```
    # Test: Alt+B and Alt+F word navigation (macOS-friendly alternative to Ctrl+Arrow)
    # Verifies: NAV-01 (Alt+B word left) and NAV-02 (Alt+F word right)

    # Initial state - cursor at line 0, col 0
    run observe=debug
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=0 ...

    # Alt+F should move forward by word (skip "# " to "Header")
    run observe=debug
    key alt+f
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=2 ...

    # Alt+F again should move to end of "Header"
    run observe=debug
    key alt+f
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=8 ...

    # Alt+F at end of line wraps to next line
    run observe=debug
    key alt+f
    ----
    -- debug:
    mode=StateDefault cursorLine=1 cursorCol=0 ...

    # Alt+B should move backward (back to previous line end)
    run observe=debug
    key alt+b
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=8 ...

    # Alt+B again moves to start of "Header"
    run observe=debug
    key alt+b
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=2 ...

    # Alt+B at word boundary moves to previous word
    run observe=debug
    key alt+b
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=0 ...
    ```

    IMPORTANT: The catwalk framework uses "alt+f" and "alt+b" (lowercase) for key simulation.
    Verify by checking existing testdata files for Alt key syntax.
  </action>
  <verify>
    Run `go test ./cmd/calcmark/tui/editor -run "TestEditorCatwalk/word_nav_comprehensive" -v`
    Test must pass showing Alt+B/F navigate correctly.
  </verify>
  <done>
    - Test file exists at testdata/word_nav_comprehensive
    - Test verifies Alt+B moves backward by word
    - Test verifies Alt+F moves forward by word
    - Test verifies line wrapping at boundaries
    - Test passes with `go test`
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate all navigation requirements</name>
  <files>No new files - validation only</files>
  <action>
    Run the complete navigation test suite to validate all 6 NAV requirements:

    1. NAV-01: Ctrl+Left and Alt+B word navigation
       - Existing: testdata/word_movement (Ctrl+Arrow)
       - New: testdata/word_nav_comprehensive (Alt+B/F)

    2. NAV-02: Ctrl+Right and Alt+F word navigation
       - Same test files as NAV-01

    3. NAV-03: Home and Ctrl+A line start
       - Existing: testdata/cursor_navigation (Home)
       - New: testdata/line_nav_ctrlae (Ctrl+A)

    4. NAV-04: End and Ctrl+E line end
       - Existing: testdata/cursor_navigation (End)
       - New: testdata/line_nav_ctrlae (Ctrl+E)

    5. NAV-05: Ctrl+Home document start
       - New: testdata/document_navigation

    6. NAV-06: Ctrl+End document end
       - New: testdata/document_navigation

    Run all tests and confirm 100% pass rate.
  </action>
  <verify>
    Run the comprehensive navigation test:
    ```bash
    go test ./cmd/calcmark/tui/editor -run "TestEditorCatwalk/(cursor_navigation|word_movement|word_nav_comprehensive|line_nav_ctrlae|document_navigation)" -v
    ```
    All tests must pass.
  </verify>
  <done>
    - All 6 navigation test files run successfully
    - No test failures or skips
    - All NAV-01 through NAV-06 requirements have test coverage
  </done>
</task>

</tasks>

<verification>
Run the full editor test suite:
```bash
go test ./cmd/calcmark/tui/editor/... -v
```

Run the complete project test suite to ensure no regressions:
```bash
task test
```
</verification>

<success_criteria>
1. `go test ./cmd/calcmark/tui/editor/...` passes with no failures
2. Alt+B works identically to Ctrl+Left for word navigation
3. Alt+F works identically to Ctrl+Right for word navigation
4. All 6 NAV requirements have dedicated test coverage
5. `task test` passes (full project tests)
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation/11-03-SUMMARY.md`
</output>
