---
phase: 11-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/calcmark/tui/editor/model.go
  - cmd/calcmark/tui/editor/testdata/line_nav_ctrlae
autonomous: true

must_haves:
  truths:
    - "Ctrl+A moves cursor to start of line (same as Home)"
    - "Ctrl+E moves cursor to end of line (same as End)"
    - "Export functionality remains accessible via alternative keybinding"
  artifacts:
    - path: "cmd/calcmark/tui/editor/model.go"
      provides: "Ctrl+A and Ctrl+E key handlers"
      contains: "KeyCtrlA"
    - path: "cmd/calcmark/tui/editor/testdata/line_nav_ctrlae"
      provides: "Catwalk test for Ctrl+A/E navigation"
      contains: "ctrl+a"
  key_links:
    - from: "model.go handleDefaultKey()"
      to: "handleHomeKey()/handleEndKey()"
      via: "KeyCtrlA/KeyCtrlE case statements"
      pattern: "case tea\\.KeyCtrl[AE]"
---

<objective>
Add Ctrl+A and Ctrl+E keybindings for line navigation, resolving the Ctrl+E export conflict.

Purpose: NAV-03 and NAV-04 require readline-style navigation (Ctrl+A for line start, Ctrl+E for line end). Currently Ctrl+E triggers export, conflicting with standard readline bindings.

Output: Ctrl+A/E work for line navigation, export moved to menu/command only, catwalk test validates behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-navigation/11-RESEARCH.md

# Key files
@cmd/calcmark/tui/editor/model.go (lines 390-520: key handling)
@cmd/calcmark/tui/editor/testdata/cursor_navigation (existing test pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Ctrl+E conflict and add Ctrl+A/E handlers</name>
  <files>cmd/calcmark/tui/editor/model.go</files>
  <action>
    1. Remove Ctrl+E from export handling (lines 395-398) - export will be command-only
    2. In handleDefaultKey() switch statement, add two new cases:
       - `case tea.KeyCtrlA:` that returns `m.handleHomeKey()` (reuse existing handler)
       - `case tea.KeyCtrlE:` that returns `m.handleEndKey()` (reuse existing handler)
    3. Place these cases near the existing Home/End cases for code organization
    4. Update the comment at line 396 to note export is command-only (remove "Ctrl+E works in all modes")

    IMPORTANT: Do NOT modify the handleHomeKey() or handleEndKey() functions - they already work correctly.
  </action>
  <verify>
    Run `go build ./cmd/calcmark/...` to verify compilation.
    Run `go test ./cmd/calcmark/tui/editor -run "TestEditorCatwalk/cursor_navigation" -v` to verify existing tests pass.
  </verify>
  <done>
    - Ctrl+A case exists and calls handleHomeKey()
    - Ctrl+E case exists and calls handleEndKey()
    - Ctrl+E no longer triggers export mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Create catwalk test for Ctrl+A/E line navigation</name>
  <files>cmd/calcmark/tui/editor/testdata/line_nav_ctrlae</files>
  <action>
    Create a new catwalk test file that verifies:
    1. Ctrl+A moves cursor to column 0 (like Home)
    2. Ctrl+E moves cursor to end of line (like End)
    3. Ctrl+A at column 0 stays at column 0
    4. Ctrl+E at end of line stays at end

    Use the existing cursor_navigation test as a template. The test should:
    - Start with default document ("# Header" on line 0, "x = 10" on line 1)
    - Move to middle of a line first (using right arrow or down+end)
    - Press ctrl+a and verify cursorCol=0
    - Press ctrl+e and verify cursorCol=line length

    Example structure:
    ```
    # Test: Ctrl+A and Ctrl+E readline-style line navigation
    # Verifies: NAV-03 (Ctrl+A -> line start) and NAV-04 (Ctrl+E -> line end)

    # Initial state
    run observe=debug
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=0 ...

    # Move to end of line 0 first
    run observe=debug
    key end
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=8 ...

    # Ctrl+A should move to column 0
    run observe=debug
    key ctrl+a
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=0 ...

    # Ctrl+E should move to end of line
    run observe=debug
    key ctrl+e
    ----
    -- debug:
    mode=StateDefault cursorLine=0 cursorCol=8 ...
    ```
  </action>
  <verify>
    Run `go test ./cmd/calcmark/tui/editor -run "TestEditorCatwalk/line_nav_ctrlae" -v`
    Test must pass showing Ctrl+A/E navigate correctly.
  </verify>
  <done>
    - Test file exists at testdata/line_nav_ctrlae
    - Test verifies Ctrl+A moves to column 0
    - Test verifies Ctrl+E moves to end of line
    - Test passes with `go test`
  </done>
</task>

</tasks>

<verification>
Run the full test suite to ensure no regressions:
```bash
go test ./cmd/calcmark/tui/editor/... -v
```

Verify the specific navigation tests pass:
```bash
go test ./cmd/calcmark/tui/editor -run "TestEditorCatwalk/(cursor_navigation|line_nav_ctrlae|word_movement)" -v
```
</verification>

<success_criteria>
1. `go test ./cmd/calcmark/tui/editor/...` passes with no failures
2. Ctrl+A moves cursor to start of line (verified by catwalk test)
3. Ctrl+E moves cursor to end of line (verified by catwalk test)
4. Export no longer triggered by Ctrl+E (code inspection confirms removal)
5. NAV-03 and NAV-04 requirements are fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation/11-01-SUMMARY.md`
</output>
