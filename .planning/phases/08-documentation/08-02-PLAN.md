---
phase: 08-documentation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "New user understands what CalcMark is from first 3 sentences"
    - "User can copy-paste installation command and run it"
    - "Quick start example is self-contained and runnable"
    - "Screenshot visible showing TUI layout"
    - "Links to detailed docs and examples exist"
  artifacts:
    - path: "README.md"
      provides: "User-facing project documentation"
      min_lines: 80
      contains: "brew install"
  key_links:
    - from: "README.md installation section"
      to: "GitHub releases"
      via: "download links"
      pattern: "github.com/CalcMark/go-calcmark/releases"
    - from: "README.md examples section"
      to: "testdata/examples/"
      via: "relative links"
      pattern: "testdata/examples"
    - from: "README.md learn more section"
      to: "docs/README.md"
      via: "relative link"
      pattern: "docs/README.md"
---

<objective>
Rewrite README.md for new user onboarding

Purpose: Enable a new user to understand, install, and start using CalcMark within 5 minutes
Output: Complete README.md with what/install/quickstart/examples/learn more structure
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation/08-CONTEXT.md
@.planning/phases/08-documentation/08-RESEARCH.md
@.planning/phases/08-documentation/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with user-focused structure</name>
  <files>README.md</files>
  <action>
Replace the current library-focused README with a user-focused README following this exact structure:

```markdown
# CalcMark

**Calculations embedded in markdown documents.**

![CalcMark TUI](docs/images/hero.gif)

CalcMark is a terminal-based calculation notepad. Write your thinking in plain text, add calculations that reference each other, and watch results update as you type.

Unlike spreadsheets, CalcMark files are human-readable, diffable, and live in your terminal.

## Installation

**macOS/Linux (Homebrew):**

```bash
brew install calcmark/tap/calcmark
```

**Download binary:**

| Platform | Download |
|----------|----------|
| macOS (Apple Silicon) | [calcmark_VERSION_darwin_arm64.tar.gz](https://github.com/CalcMark/go-calcmark/releases/latest) |
| macOS (Intel) | [calcmark_VERSION_darwin_amd64.tar.gz](https://github.com/CalcMark/go-calcmark/releases/latest) |
| Linux (x64) | [calcmark_VERSION_linux_amd64.tar.gz](https://github.com/CalcMark/go-calcmark/releases/latest) |
| Linux (arm64) | [calcmark_VERSION_linux_arm64.tar.gz](https://github.com/CalcMark/go-calcmark/releases/latest) |
| Windows (x64) | [calcmark_VERSION_windows_amd64.zip](https://github.com/CalcMark/go-calcmark/releases/latest) |

After downloading, extract and move `cm` to a directory in your PATH.

## Quick Start

1. Create a file called `budget.cm`:

```
# Monthly Budget

income = $5000
rent = $1500
savings_rate = 20%
savings = income * savings_rate
remaining = income - rent - savings
```

2. Open in the TUI editor:

```bash
cm budget.cm
```

3. Or evaluate from command line:

```bash
cm eval budget.cm
```

![CalcMark TUI](docs/images/tui-screenshot.png)

## Examples

Explore example files to see CalcMark in action:

- [Budget planning](testdata/examples/budget.cm) - Monthly budget with income, expenses, savings
- [Unit conversion](testdata/examples/unit-conversion.cm) - Converting between units
- [Capacity planning](testdata/examples/engineering.cm) - Engineering calculations with constants

Run any example:

```bash
cm testdata/examples/budget.cm
```

## Features

- **Variables flow downward** - Define once, reference anywhere below
- **Units are first-class** - `5 miles in km`, `20 celsius in fahrenheit`
- **Currencies** - `$100`, `50 EUR`, automatic formatting
- **Percentages** - `savings_rate = 20%`, then `income * savings_rate`
- **Functions** - `avg()`, `sqrt()`, `capacity()`, and more
- **YAML front matter** - Define document-level constants

## Help Commands

```bash
cm help              # General help
cm help functions    # List all functions with descriptions
cm help constants    # List built-in constants
```

Press F1 in the TUI editor for keybindings.

## Learn More

- [User Guide](docs/README.md) - Complete documentation with examples
- [Language Spec](spec/LANGUAGE_SPEC.md) - Formal language specification

## License

MIT
```

Key requirements for the README:
1. Hero GIF at top (after title and tagline)
2. "What is CalcMark" in first 2-3 sentences - position as "calculation notepad" not "Jupyter alternative"
3. Brief motivation (vs spreadsheets) - one sentence only
4. Installation: Homebrew one-liner first, then binary download table
5. Quick start: Create file + open in TUI + CLI alternative
6. Screenshot after quick start section
7. Examples: Link to testdata/examples/ with brief descriptions
8. Features: Bullet list of key capabilities
9. Help commands: Show CLI help options
10. Learn more: Link to docs/README.md (detailed guide) and spec

Do NOT include:
- Scoop installation (removed from scope)
- Contributing guide (deferred per CONTEXT.md)
- Architecture diagrams (that's developer docs, not user docs)
- Go library usage examples (that belongs in a separate DEVELOPERS.md or in the godoc)
  </action>
  <verify>
1. README.md exists with new content
2. Hero GIF reference: `grep -q "docs/images/hero.gif" README.md`
3. Screenshot reference: `grep -q "docs/images/tui-screenshot.png" README.md`
4. Homebrew install: `grep -q "brew install calcmark/tap/calcmark" README.md`
5. Example links: `grep -q "testdata/examples" README.md`
6. User guide link: `grep -q "docs/README.md" README.md`
  </verify>
  <done>README.md completely rewritten with user-focused structure, hero GIF at top, installation, quick start, examples, and links to detailed docs</done>
</task>

<task type="auto">
  <name>Task 2: Verify all README links work</name>
  <files>README.md</files>
  <action>
Verify all relative links in README.md point to existing files:

1. Check docs/images/hero.gif exists
2. Check docs/images/tui-screenshot.png exists
3. Check testdata/examples/budget.cm exists
4. Check testdata/examples/unit-conversion.cm exists
5. Check testdata/examples/engineering.cm exists
6. Check docs/README.md exists
7. Check spec/LANGUAGE_SPEC.md exists

If any file is missing, note it as an issue (should not happen if Plan 01 completed successfully).

External links (GitHub releases) cannot be verified until first release is created.
  </action>
  <verify>
```bash
cd /Users/bitsbyme/projects/go-calcmark
test -f docs/images/hero.gif && \
test -f docs/images/tui-screenshot.png && \
test -f testdata/examples/budget.cm && \
test -f testdata/examples/unit-conversion.cm && \
test -f testdata/examples/engineering.cm && \
test -f docs/README.md && \
test -f spec/LANGUAGE_SPEC.md && \
echo "All links valid"
```
  </verify>
  <done>All relative links in README.md point to existing files</done>
</task>

</tasks>

<verification>
- [ ] README.md has hero GIF near top
- [ ] README.md explains what CalcMark is in first 3 sentences
- [ ] Installation section has Homebrew one-liner
- [ ] Installation section has binary download links
- [ ] Quick start shows create-file, open-tui, cli-eval workflow
- [ ] Screenshot visible in README
- [ ] Examples link to testdata/examples/
- [ ] Features list covers key capabilities
- [ ] Help commands documented
- [ ] Links to docs/README.md and spec/LANGUAGE_SPEC.md
- [ ] All relative links resolve to existing files
</verification>

<success_criteria>
1. A new user can understand what CalcMark is from reading the first section
2. Installation instructions are copy-paste ready (Homebrew or download)
3. Quick start example can be run immediately after installation
4. README contains at least one screenshot showing TUI layout
5. README links to detailed documentation and examples
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation/08-02-SUMMARY.md`
</output>
