name: PR Velocity Metrics

on:
  pull_request:
    types: [closed]
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to annotate'
        required: true
        type: number

permissions:
  contents: read
  pull-requests: write

jobs:
  metrics:
    name: Annotate PR with Velocity Metrics
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.merged == true &&
       !contains(fromJSON('["dependabot[bot]","renovate[bot]"]'), github.actor))
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install gh-velocity
        run: gh extension install dvhthomas/gh-velocity --pin v0.1.1
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine PR number
        id: pr
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_NUMBER: ${{ inputs.pr_number }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "number=$INPUT_NUMBER" >> "$GITHUB_OUTPUT"
          else
            echo "number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate metrics
        id: metrics
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ITEM_NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          gh velocity pr "$ITEM_NUMBER" -r markdown > /tmp/velocity-metrics.md 2>/dev/null || true
          if [ ! -s /tmp/velocity-metrics.md ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"

      - name: Update PR body
        if: steps.metrics.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ITEM_NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          # Get current body to a file (avoids shell injection via body content)
          gh pr view "$ITEM_NUMBER" --json body --jq '.body // ""' > /tmp/current-body.txt

          # Build the metrics block
          {
            echo '<!-- gh-velocity-start -->'
            echo '---'
            echo '<details>'
            echo '<summary>Velocity Metrics</summary>'
            echo ''
            cat /tmp/velocity-metrics.md
            echo ''
            echo '</details>'
            echo '<!-- gh-velocity-end -->'
          } > /tmp/metrics-block.txt

          # Strip old sentinel block if present, then append new one
          sed '/<!-- gh-velocity-start -->/,/<!-- gh-velocity-end -->/d' /tmp/current-body.txt > /tmp/new-body.txt
          echo '' >> /tmp/new-body.txt
          cat /tmp/metrics-block.txt >> /tmp/new-body.txt

          gh pr edit "$ITEM_NUMBER" --body-file /tmp/new-body.txt
